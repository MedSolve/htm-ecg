% storage path
storePath = 'data/nupi.source.csv';

% Get the folder for data source
source = uigetdir;

% get files of source
list = dir([source '/*.xml']);

% loop the source
for i=1:length(list)
    
    list(i)
end


% I xmlstruct er fielded